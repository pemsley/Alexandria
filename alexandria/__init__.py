"""Alexandria — a personal library of scientific PDFs.

GTK4 desktop application; metadata enrichment via OpenAlex and
CrossRef; sidecar JSON next to each PDF as the source of truth,
SQLite index as a regenerated cache."""

__version__ = "0.1.0"
